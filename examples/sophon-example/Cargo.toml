[package]
name = "sophon-example"
version = "0.1.0"
edition = "2021"

[dependencies]
sophon-runtime = { path = "../../sophon-runtime", features = ["ffi"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
